---
title: nookdb
description: A database in Electron means better-sqlite3 + hand-written IPC + hand-rolled reactivity. NookDB gives you all three in one API.
template: splash
hero:
  tagline: A database in Electron means better-sqlite3 + hand-written IPC + hand-rolled reactivity. NookDB gives you all three in one API.
  actions:
    - text: Quick start
      link: /quick-start/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/omerarslan0/nookdb
      icon: external
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Why nookdb?

<CardGrid>
  <Card title="Schema-first" icon="document">
    Define data once; types, validation, and indexes flow from a single source.
  </Card>
  <Card title="Reactive by default" icon="bars">
    Every query has a `.live()` variant. UI stays in sync with the database.
  </Card>
  <Card title="Multi-process built in" icon="puzzle">
    Electron renderers use the same API as the main process via a typed proxy — no IPC code.
  </Card>
  <Card title="Rust-powered" icon="rocket">
    ACID transactions on redb. fsync where it matters.
  </Card>
</CardGrid>
